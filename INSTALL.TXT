感谢“雨打芭蕉”提供的安装说明
https://github.com/mozilla/geckodriver/releases
安装geckodriver
$ git clone https://github.com/pchaosgit/stock-by-William-O-Neil.git
$ cd stock-by-William-O-Neil/
$ python -m pip install -r requirements.txt
$ python setup.py install
$ git clone https://github.com/pchaos/wanggeService.git
$ cd wanggeService/
$ git branch -a
$ git checkout -b dev origin/dev
$ python -m pip install -r requirements.txt
#编辑initdata.py,内容如下;
$ cat initdata.py
import sys; print('Python %s on %s' % (sys.version, sys.platform))
import os
os.environ.setdefault("DJANGO_SETTINGS_MODULE", "wanggeService.settings")

import django; print('Django %s' % django.get_version())
if 'setup' in dir(django): django.setup()

import pandas as pd
import numpy as np
from stocks.models import *
import QUANTAXIS as qa
import datetime

from stocks.models import Stocktradedate
# 交易日期
qs = Stocktradedate.getlist()
if qs.count() > 0:
    qs.delete()
qs = Stocktradedate.importList()
print('交易日期：{}'.format(qs.count()))

#基础数据
from stocks.models import Listing
qs = Listing.getlist()
if qs.count() > 0:
    qs.delete()
qs = Listing.importStockListing()
print('导入股票：{}'.format(qs.count()))
qs = Listing.getlist('index')
if qs.count() > 0:
    qs.delete()
qs = Listing.importIndexListing()
print('导入指数：{}'.format(qs.count()))
$ python initdata.py
$ python updateStockData.py
$ python manage.py createsuperuser
$ python manage.py runserver

